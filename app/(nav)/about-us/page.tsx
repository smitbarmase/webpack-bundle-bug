export default function Page() {
  // No swiper used on this page. Still swiper code can be seen in network on this page load.
  // If no query link is use anywhere, this doesn't happen.
  return <main>About page</main>;
}
